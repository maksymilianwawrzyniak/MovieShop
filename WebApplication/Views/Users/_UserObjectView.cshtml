@model User

<div class="container-fluid">
    <div>
        <div id="buttons" class="row pl-1">
            <a asp-controller="Admin" asp-action="RemoveUser" asp-route-id="@Model.Id">
                <div class="col-6 mb-0">
                   Delete
                </div>
            </a>
        </div>
    </div>
</div>